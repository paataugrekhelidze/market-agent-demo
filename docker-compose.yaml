services:
  db:
    image: pgvector/pgvector:pg17
    container_name: agent_cluster_db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: local_dev_password
      POSTGRES_DB: agent_warehouse
    volumes:
      - pg_data:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d agent_warehouse"]
      interval: 5s
      timeout: 5s
      retries: 5

  init-db:
    image: ghcr.io/astral-sh/uv:python3.12-trixie-slim
    working_dir: /app
    depends_on:
      db:
        condition: service_healthy
    environment:
      POSTGRES_HOST: db
      POSTGRES_PORT: "5432"
      POSTGRES_DB: agent_warehouse
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: local_dev_password
      DATA_DIR: /app/data
      HEADLINE_SAMPLE_SIZE: "20000"
      COHERE_API_KEY: ${COHERE_API_KEY}
      UV_LINK_MODE: copy
    volumes:
      - .:/app
      # prevent conflict with local .venv
      - init_db_venv:/app/.venv
      - uv_cache:/root/.cache/uv
    command: sh -c "uv sync --frozen && uv run --frozen python init_db.py"

  prefect-server:
    image: prefecthq/prefect:3-latest
    command: prefect server start --host 0.0.0.0
    ports:
      - "4200:4200"
    depends_on:
      db:
        condition: service_healthy
    environment:
      PREFECT_SERVER_ANALYTICS_ENABLED: "false"
      # point prefect server to the postgresql
      PREFECT_SERVER_DATABASE_CONNECTION_URL: postgresql+asyncpg://postgres:local_dev_password@db:5432/prefect
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:4200/api/health')\""]
      interval: 5s
      timeout: 5s
      retries: 10

  agent:
    build: .
    container_name: agent_runner
    ports:
      - "8080:8080"
    depends_on:
      db:
        condition: service_healthy
      prefect-server:
        condition: service_healthy
      init-db:
        condition: service_completed_successfully
    environment:
      POSTGRES_HOST: db
      POSTGRES_PORT: "5432"
      POSTGRES_DB: agent_warehouse
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: local_dev_password
      OPENAI_API_KEY: ${OPENAI_API_KEY}
      COHERE_API_KEY: ${COHERE_API_KEY}
      HF_HOME: /root/.cache/huggingface
      UV_LINK_MODE: copy
      PREFECT_SERVER_ANALYTICS_ENABLED: "false"
      PREFECT_API_URL: http://prefect-server:4200/api
    volumes:
      - agent_venv:/app/.venv
      - uv_cache:/root/.cache/uv
      - hf_cache:/root/.cache/huggingface
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8080/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  pg_data:
  uv_cache:
  init_db_venv:
  agent_venv:
  hf_cache: