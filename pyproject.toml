[project]
name = "market-agent-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "prefect>=3.7.1",
    "prefect-aws>=0.7.9",
    "pydantic-ai>=1.100.0"
]
